# Backend Environment Variables for Render
# Copy these to your Render service environment variables

# MongoDB Connection (use MongoDB Atlas)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ats-resume-builder?retryWrites=true&w=majority

# JWT Secret (generate a strong secret)
JWT_SECRET=your-super-secret-jwt-key-here-minimum-32-characters

# Admin Creation Secret (for free tier admin setup)
ADMIN_CREATION_SECRET=create-admin-2024

# Google Gemini AI API Key
GEMINI_API_KEY=your-gemini-api-key

# Firebase Admin SDK (for server-side auth)
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=your-firebase-client-email
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Private-Key-Here\n-----END PRIVATE KEY-----"

# Email Configuration (for forgot password)
EMAIL_USER=your-gmail-address@gmail.com
EMAIL_PASS=your-gmail-app-password

# Email Configuration Instructions:
# 1. Enable 2-Factor Authentication on your Gmail account
# 2. Go to Google Account Settings > Security > App Passwords
# 3. Generate a new app password for "Mail"
# 4. Use this 16-character app password (not your regular Gmail password)
# Example: EMAIL_PASS=abcd efgh ijkl mnop (remove spaces)

# Frontend URL (your Vercel deployment)
FRONTEND_URL=https://your-vercel-app.vercel.app

# Node Environment
NODE_ENV=production

# Port (Render will set this automatically)
PORT=5000